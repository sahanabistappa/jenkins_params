import sys
import json
import ast

if __name__=="__main__":
  cmd_arg=sys.argv[1]
#   user_config=json.loads(cmd_arg)
  print(cmd_arg)
  print(type(cmd_arg))
#   print(user_config)
#   print(type(user_config))
